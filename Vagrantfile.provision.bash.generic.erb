# -*- mode: ruby -*-
# vi: set ft=ruby :

Vagrant.configure(2) do |config|
  config.vm.box_check_update = false

  # vbox template for all vagranth instances
  config.vm.provider "virtualbox" do |vb|
    vb.gui = false
    vb.memory = "512"
    vb.cpus = 2
  end
             config.vm.define "vg-alpine-01" do |k8scluster|
                k8scluster.vm.box = "generic/alpine39"
                <%# k8scluster.vm.box_version = "3.0.28" %>
                k8scluster.vm.hostname = "vg-alpine-01"
                k8scluster.vm.network "private_network", ip: "192.168.50.14"
                #Disabling the default /vagrant share can be done as follows:
                k8scluster.vm.synced_folder ".", "/vagrant", disabled: true
                k8scluster.vm.provider "virtualbox" do |vb|
                    vb.name = "vbox-alpine-01"
                    vb.memory = "4096"
                end

                k8scluster.vm.provision :shell, path: "provision/bootstrap_alpine.sh"

                # k8scluster.vm.provision "ansible_local" do |ansible|
                #   # https://www.vagrantup.com/docs/provisioning/ansible_common.html Shared Ansible Options
                #   ansible.become = true
                #   ansible.compatibility_mode = "2.0"
                #   ansible.version = "2.9.9"
                #   ansible.extra_vars = {
                #           node_ip: "192.168.50.12"
                #       }
                #   ansible.playbook = "provisioning/deploy.yml"
                # end

              end

             config.vm.define "vg-gentoo-02" do |k8scluster|
                k8scluster.vm.box = "generic/gentoo"
                k8scluster.vm.hostname = "vg-gentoo-02"
                <%# k8scluster.vm.box_version = "3.0.28" %>
                k8scluster.vm.network "private_network", ip: "192.168.50.15"
                #Disabling the default /vagrant share can be done as follows:
                k8scluster.vm.synced_folder ".", "/vagrant", disabled: true
                k8scluster.vm.provider "virtualbox" do |vb|
                    vb.name = "vbox-gentoo-02"
                    vb.memory = "4096"
                end

                k8scluster.vm.provision :shell, path: "provision/bootstrap_gentoo.sh"

                # k8scluster.vm.provision "ansible_local" do |ansible|
                #   # https://www.vagrantup.com/docs/provisioning/ansible_common.html Shared Ansible Options
                #   ansible.become = true
                #   ansible.compatibility_mode = "2.0"
                #   ansible.version = "2.9.9"
                #   ansible.extra_vars = {
                #           node_ip: "192.168.50.12"
                #       }
                #   ansible.playbook = "provisioning/deploy.yml"
                # end

              end

             config.vm.define "vg-dragonfly-03" do |k8scluster|
                k8scluster.vm.box = "dragonflybsd/dragonfly520"
                k8scluster.vm.hostname = "vg-dragonfly-03"
                k8scluster.vm.box_version = "0.0.1"                
                k8scluster.vm.network "private_network", ip: "192.168.48.108"                
                #Disabling the default /vagrant share can be done as follows:
                k8scluster.vm.synced_folder ".", "/vagrant", disabled: true
                k8scluster.vm.provider "virtualbox" do |vb|
                    vb.name = "vbox-dragonfly-03"
                    vb.memory = "4096"
                end

                k8scluster.vm.provision :shell, path: "provision/bootstrap.sh"

                # k8scluster.vm.provision "ansible_local" do |ansible|
                #   # https://www.vagrantup.com/docs/provisioning/ansible_common.html Shared Ansible Options
                #   ansible.become = true
                #   ansible.compatibility_mode = "2.0"
                #   ansible.version = "2.9.9"
                #   ansible.extra_vars = {
                #           node_ip: "192.168.50.12"
                #       }
                #   ansible.playbook = "provisioning/deploy.yml"
                # end

              end

             config.vm.define "vg-hardenedbsd-04" do |k8scluster|
                k8scluster.vm.box = "generic/hardenedbsd12"
                k8scluster.vm.hostname = "vg-hardenedbsd-04"
                <%# k8scluster.vm.network "private_network", ip: "192.168.50.15" %>
                #Disabling the default /vagrant share can be done as follows:
                k8scluster.vm.synced_folder ".", "/vagrant", disabled: true
                k8scluster.vm.provider "virtualbox" do |vb|
                    vb.name = "vbox-hardenedbsd-04"
                    vb.memory = "4096"
                end

                k8scluster.vm.provision :shell, path: "provision/bootstrap.sh"

                # k8scluster.vm.provision "ansible_local" do |ansible|
                #   # https://www.vagrantup.com/docs/provisioning/ansible_common.html Shared Ansible Options
                #   ansible.become = true
                #   ansible.compatibility_mode = "2.0"
                #   ansible.version = "2.9.9"
                #   ansible.extra_vars = {
                #           node_ip: "192.168.50.12"
                #       }
                #   ansible.playbook = "provisioning/deploy.yml"
                # end

              end

end
